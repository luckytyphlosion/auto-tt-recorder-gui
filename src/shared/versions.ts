const packageJson = require("../../package.json");

export const AUTO_TT_RECORDER_VERSION = `v${packageJson.autoTTRecorderVersion}`;
export const AUTO_TT_RECORDER_FOLDER_NAME = `auto-tt-recorder_${AUTO_TT_RECORDER_VERSION}_for_gui`
export const AUTO_TT_RECORDER_GUI_VERSION = `v${packageJson.version}`;
export const DOLPHIN_VERSION = `v${packageJson.dolphinVersion}`;
